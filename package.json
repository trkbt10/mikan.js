{
  "name": "budou.js",
  "version": "1.0.3",
  "description": "Japanese Line Break Organizer",
  "main": "lib/budou.js",
  "scripts": {
    "prepublish": "$(npm bin)/babel src/budou.js --out-file lib/budou.js && $(npm bin)/browserify ./src/budou-web.js -o ./lib/budou-web.js -t babelify -t uglifyify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "assert": "^1.4.1",
    "babel-cli": "^6.14.0",
    "babel-polyfill": "^6.13.0",
    "babel-preset-es2015": "^6.14.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1",
    "uglifyify": "^3.0.2"
  }
}
